<?php

// AdminUserSeeder removed — legacy `adm_u` table is no longer managed.
// Kept the file as a marker. The `LaravelAdminSeeder` manages the active `users` table.

namespace Database\Seeders;

use Illuminate\Database\Seeder;

class AdminUserSeeder extends Seeder
{
    public function run(): void
    {
        // intentionally left blank
    }
}
